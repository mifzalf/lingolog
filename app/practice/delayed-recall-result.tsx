import { useEffect, useState } from 'react';
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { router, useLocalSearchParams } from 'expo-router';
import { PaperScreen } from '../../src/components/PaperScreen';
import { useDatabase } from '../../src/db/DatabaseProvider';
import { getDelayedRecallResult } from '../../src/features/practice/delayed-recall.repository';
import { useTheme } from '../../src/theme/ThemeProvider';
import { radius, ThemeColors } from '../../src/theme/tokens';
export default function DelayedRecallResultScreen() {
  const database = useDatabase(); const { colors } = useTheme(); const styles = createStyles(colors); const { sessionId } = useLocalSearchParams<{ sessionId: string }>(); const [result, setResult] = useState<Awaited<ReturnType<typeof getDelayedRecallResult>>>(); const [loading, setLoading] = useState(true);
  useEffect(() => { getDelayedRecallResult(database, Number(sessionId)).then(setResult).catch(console.error).finally(() => setLoading(false)); }, [database, sessionId]);
  if (loading) return <View style={styles.center}><ActivityIndicator color={colors.primary} /></View>; if (!result) return <View style={styles.center}><Text style={styles.error}>Hasil sesi tidak ditemukan.</Text><Pressable onPress={() => router.dismissTo('/practice')}><Text style={styles.link}>Kembali</Text></Pressable></View>;
  const accuracy = result.answered ? Math.round(result.correct / result.answered * 100) : 0; const seconds = Math.max(1, Math.round(result.session.durationMs / 1000));
  return <PaperScreen contentContainerStyle={styles.content}><View style={styles.mark}><Ionicons name="hourglass-outline" size={36} color={colors.primary} /></View><Text style={styles.note}>INGAT LAGI SELESAI</Text><Text style={styles.title}>{accuracy >= 70 ? 'Ingatan tertundamu kuat.' : 'Jeda berhasil menemukan yang rapuh.'}</Text><Text style={styles.subtitle}>Mengingat setelah kata menghilang melatih kemampuan mengambil informasi, bukan sekadar mengenalinya.</Text><View style={styles.hero}><Text style={styles.heroValue}>{accuracy}%</Text><Text style={styles.heroLabel}>jawaban tepat</Text><View style={styles.rule} /><Text style={styles.meta}>{result.answered} ronde · {seconds} detik · rata-rata {(result.averageMs / 1000).toFixed(1)} detik menjawab</Text></View><View style={styles.stats}><View style={styles.stat}><Text style={styles.statValue}>{result.correct}</Text><Text style={styles.statLabel}>Tepat</Text></View><View style={styles.stat}><Text style={styles.statValue}>{result.answered - result.correct}</Text><Text style={styles.statLabel}>Belum tepat</Text></View></View><Pressable onPress={() => router.replace({ pathname: '/practice/setup', params: { mode: 'delayed_recall' } })} style={styles.primary}><Ionicons name="refresh" size={19} color={colors.primaryInk} /><Text style={styles.primaryText}>Ingat lagi</Text></Pressable><Pressable onPress={() => router.dismissTo('/practice')} style={styles.secondary}><Text style={styles.secondaryText}>Kembali ke Latihan</Text></Pressable></PaperScreen>;
}
const createStyles = (colors: ThemeColors) => StyleSheet.create({ center: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 10, backgroundColor: colors.paper }, error: { color: colors.danger, fontWeight: '800' }, link: { color: colors.primary, fontWeight: '900' }, content: { alignItems: 'center', paddingTop: 70 }, mark: { width: 72, height: 72, alignItems: 'center', justifyContent: 'center', borderRadius: 36, backgroundColor: colors.primarySoft }, note: { color: colors.primary, fontSize: 10, fontWeight: '900', letterSpacing: 1.3, marginTop: 15 }, title: { color: colors.ink, fontSize: 28, lineHeight: 35, fontWeight: '900', textAlign: 'center', marginTop: 7 }, subtitle: { color: colors.inkMuted, fontSize: 13, lineHeight: 20, textAlign: 'center', marginTop: 9 }, hero: { width: '100%', alignItems: 'center', marginTop: 25, padding: 23, borderRadius: radius.lg, backgroundColor: colors.paperRaised, borderWidth: 1, borderColor: colors.rule }, heroValue: { color: colors.ink, fontSize: 45, fontWeight: '900' }, heroLabel: { color: colors.primary, fontSize: 12, fontWeight: '900' }, rule: { width: 62, height: 3, marginVertical: 17, borderRadius: 3, backgroundColor: colors.highlight }, meta: { color: colors.inkMuted, fontSize: 11, textAlign: 'center' }, stats: { width: '100%', flexDirection: 'row', gap: 9, marginTop: 11 }, stat: { flex: 1, alignItems: 'center', padding: 14, borderRadius: radius.md, backgroundColor: colors.paperRaised, borderWidth: 1, borderColor: colors.rule }, statValue: { color: colors.ink, fontSize: 22, fontWeight: '900' }, statLabel: { color: colors.inkMuted, fontSize: 10, fontWeight: '800' }, primary: { width: '100%', minHeight: 54, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 8, marginTop: 20, borderRadius: radius.md, backgroundColor: colors.primary }, primaryText: { color: colors.primaryInk, fontWeight: '900' }, secondary: { minHeight: 47, justifyContent: 'center', paddingHorizontal: 20 }, secondaryText: { color: colors.primary, fontWeight: '900' } });
